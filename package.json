{
  "name": "postman",
  "version": "1.0.0",
  "description": "postman collection in jenkins",

  "scripts": {
    "test": "newman run Address.postman_collection.json --reporters cli,junit --reporter-junit-export newman.xml"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "newman": "^3.5.2"
  }
}
